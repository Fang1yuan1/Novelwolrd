-- شغّل الأمر ده مرة واحدة بس في Supabase → SQL Editor
-- الهدف: عمود يحسب عدد أحرف كل فصل تلقائيًا، فصفحة الفهرس تعرض «عدد الأحرف» بجنب كل فصل
-- من غير ما تسحب نص كل الفصول من قاعدة البيانات (وده اللي كان بيبطّئ الصفحة).
-- لو ما شغّلتوش الفهرس بيشتغل سريع بس بيخفي عدد الأحرف.
-- بعد التشغيل يظهر العدد خلال نحو 5 دقايق (أو أعد نشر الموقع لو تبغاه فورًا).

alter table chapters
  add column if not exists word_count integer
  generated always as (char_length(content)) stored;
