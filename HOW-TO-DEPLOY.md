# خطوات التفعيل — حماية الموقع من البوتات والنسخ اليدوي

## الملفات اللي اتضافت/اتعدّلت في المشروع ده

| الملف | جديد/معدّل | وظيفته |
|---|---|---|
| `middleware.ts` | جديد (في روت المشروع) | Rate limiting + حظر أدوات السحب المعروفة (curl, python-requests...) |
| `components/Footer.tsx` | معدّل | لينك مخفي (honeypot) بيحظر أي بوت يزوره فورًا |
| `app/globals.css` | معدّل | كلاس `.chapter-no-copy` — بيمنع تحديد النص بصريًا من غير أي تغيير شكلي |
| `lib/useCopyProtection.ts` | جديد | هوك بيمنع Ctrl+C / كليك يمين / قص داخل نص الفصل بس |
| `components/chapter/ChapterPageClient.tsx` | معدّل | ربط الحماية بنسخة الديسكتوب من صفحة الفصل |
| `components/chapter/MobileChapterReader.tsx` | معدّل | ربط الحماية بنسخة الموبايل من صفحة الفصل |

كل التعديلات دي **مش بتغيّر شكل الموقع خالص** — نفس الألوان، الخط، المسافات، كل حاجة زي ما هي.

---

## خطوات الرفع (نفس الخطوات لأي تعديل جاي بعد كده)

### 1. فك الضغط عن الـ zip
فك `Novelworld-protected.zip` في مكان على جهازك، وادّيه يستبدل مجلد مشروعك الحالي (أو انسخ الملفات اللي في الجدول فوق يدويًا لو عايز تحافظ على أي تعديلات تانية عملتها بنفسك).

### 2. (اختياري) جرّب محليًا الأول
```bash
npm install
npm run dev
```
افتح `http://localhost:3000` على أي فصل، وجرب:
- تحديد النص بالماوس → المفروض ميتحددش
- كليك يمين → المفروض معمول لهملش حاجة
- Ctrl+C → المفروض ملهاش تأثير

### 3. ارفع التعديلات على GitHub
من جوه مجلد المشروع، في الترمينال:
```bash
git add .
git commit -m "إضافة حماية ضد البوتات والنسخ اليدوي"
git push
```

> لو المشروع مش مربوط بـ Git لسه، أو مش عارف الأوامر دي، قولي وهساعدك خطوة خطوة.

### 4. Vercel هيعمل Deploy تلقائي
بعد الـ push، Vercel بيكتشف التحديث وينشره لوحده خلال دقيقة أو اتنين — تقدر تتابع التقدّم من:
**Vercel Dashboard → مشروعك → Deployments**

### 5. تأكيد إن الحماية شغالة فعليًا (بعد النشر)
- **النسخ اليدوي**: افتح فصل حقيقي على موقعك اللايف، جرب التحديد/النسخ زي فوق.
- **الـ Rate Limiting**: اعمل refresh سريع لصفحة فصل حوالي 15-20 مرة متتالية — لازم تاخد رسالة "بطّئ شوية" (429).
- **حظر أدوات السحب**: من أي جهاز فيه terminal:
  ```bash
  curl -I https://موقعك.com
  ```
  المفروض ترجع `403 Forbidden`.

---

## ملاحظة مهمة
لو عندك أي تعديلات تانية عملتها بنفسك على نفس الملفات (خصوصًا `Footer.tsx` أو `globals.css`) بعد ما اخدت النسخة الأصلية مني، **راجعها الأول قبل الاستبدال** عشان متضيعش شغلك — أو ابعتلي التعديلات الجديدة وهدمجها مع الحماية.
